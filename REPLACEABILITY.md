<!--
本文件由 tools/sanitize_for_publish.py 自动脱敏生成。
用户名路径已替换为环境变量形式，本机音频端点 GUID 已替换为占位符。
-->

# INZONE H6 Air：专用声卡 + 专用驱动，能不能被"完美替代"？

一句话结论：
**调音部分可以做到 100% 数学等价复刻；但"声卡 + 驱动"这一整套硬件方案，软件替代不了——你只能换一块声卡，不能用软件把它变没。**

换句话说：**你可以扔掉 INZONE Hub，但扔不掉那个盒子里的 DAC 和 ADC。**

---

## 一、先纠正一个前提

你说的是"耳机可以通过电脑里的软件调音"。更准确的说法是：

> 调音**本来就只存在于电脑软件里**。耳机本体没有，声卡里也没有。

依据（本机实测）：

- 耳机本体 `MDR-G600` 是 3.5 mm 4 极 TRRS 纯模拟，无固件、无芯片。
- USB-C 音频盒（`YY2990`，`VID_054C` / `PID_0FC0`·`0FC1`）只做 **DAC + 麦克风 ADC**，不参与调音，不存储调音参数。
- 真正的 DSP 是 `INZONEVirtualizer.dll`，被 `inzoneapo.inf` 注册成 Windows 的 **SFX（Stream Effect）音频处理对象**，跑在 `audiodg.exe` 里处理 PC 上的音频流。

所以"声卡里藏着调音、需要逆向声卡"这条路**在物理层就不存在**，不是难，是没有。

---

## 二、可替代性矩阵

分三层看，别混为一谈。

### 第 1 层：软件层的调音 —— 可 100% 等效复刻

| 功能 | 参数来源 | 可替代性 | 替换方式 |
|---|---|---|---|
| 耳机 10 段参数均衡 | `%APPDATA%\...\APO\{<耳机输出端点GUID>}.yaml` | ✅ **完美**（系数已验证到小数 8 位） | Equalizer APO / SoundSource / 任意卷积器 |
| 麦克风 7 段语音均衡 | `ProgramData\...\APO\Chat.yaml` | ✅ **完美** | 同上，挂在输入通道 |
| 三段音调（125/500/4k，mid +0.5） | 同上 | ✅ 完美 | 3 个 PK 滤波器 |
| 内部余量管理 amp1 −18 dB / amp2 +18 dB | 同上 | ✅ 完美 | 净增益 0，可忽略 |
| ALC 自动电平（阈值 −18 dB） | `alc.cfg` | ⚠️ 可复刻但**不是 EQ** | 需压缩器/限幅器，动态行为 |
| 峰值限幅 −20 dBFS | `peak_limiting.cfg` | ⚠️ 同上 | 需限幅器 |
| DRC 动态范围控制 | YAML（当前关闭） | ⚠️ 同上 | 需多段压缩器 |
| **360 空间音效** | `standard_hrtf.hki` + `YY2990_standard.ba` | ⚠️ 可解析/可测量，非"抄参数" | 离线解 `.hki` 系数，或扫频反卷积 |

### 第 2 层：硬件层 —— 软件替代不了

| 组件 | 谁在做 | 可替代性 | 说明 |
|---|---|---|---|
| DAC 数模转换 | 音频盒硬件 | ❌ **不可能** | 软件变不出模拟输出，只能换一块 DAC |
| 麦克风 ADC | 音频盒硬件 | ❌ **不可能** | 同上 |
| 专有 KS 音频驱动 `INZONEHeadset.sys` | Sony 自写（**非** Windows 通用 `usbaudio.sys`） | ❌ | 这就是它插 Mac 上"有声但音量/行为怪"的根源 |
| 插头检测（3 极 / 4 极切换 PID） | 盒内 MCU | ❌ | 硬件行为 |
| 线控 / 音量 / 静音 HID 通道 | 盒子 | ❌ 平台相关 | Windows 上走 HID，换平台可能失效 |
| 麦克风物理特性（灵敏度、底噪、指向） | 硬件 | ❌ | — |

### 第 3 层：驱动身份 —— 分两半

| 驱动在做的事 | 可替代性 |
|---|---|
| 把 `INZONEVirtualizer.dll` 注册成设备 APO | ✅ 可替代（Equalizer APO 就是同一层，甚至可把索尼 APO 挂到别处） |
| 让音频盒被 Windows 正确识别（非标 UAC） | ❌ 不可替代，只能在 Windows 上跑 |

---

## 三、"完美"要打的两个引号

1. **滤波器等价 ≠ 整机频响等价。**
   数字域 EQ 完全相同，但一旦 3.5 mm 直连主板，末端的**输出阻抗**换了。动圈耳机的阻抗随频率变化，输出阻抗不同 → 阻尼不同 → 低频频响会变。差异通常远小于 EQ 本身，但严格说，这不是"零差异"。

2. **软件管不到模拟域。**
   出了 DAC 就是模拟信号，软件层到此为止。这也是为什么"搞个软件就等于换了声卡"在物理上不成立。

---

## 四、落到你的三个场景

| 场景 | 调音能不能拿走 | 声卡要不要换 |
|---|---|---|
| **Windows + 原装盒子** | 本来就是它工作的地方，无需替代 | 不用 |
| **Windows + 3.5 mm 直连主板** | ✅ 完美（Equalizer APO 贴 `data/equalizer_apo.txt`） | 不用，用主板声卡 |
| **macOS** | ✅ 可以（SoundSource / eqMac / 卷积 `ir_48000.wav`） | 建议 3.5 mm 直连，盒子在 Mac 上行为不保证 |
| **任意平台 + 绝对精确** | ✅ 喂卷积器 | 任何能出声的 DAC |

进阶（可选）：Windows 上可以把 **索尼自己的 APO** 挂到主板输出端点，连 360 空间音效一起带走 —— 见 `README.md` §6，需改注册表。

---

## 五、回答你的问题

- **"耳机可以通过电脑软件调音"** → 对，而且是**只能**通过电脑软件调音，硬件不参与。
- **"专用声卡可以被完美替代"** → 不准确。声卡是 DAC/ADC，软件替代不了模拟转换；但**它对音色没有任何贡献，所以换掉它对调音零影响**。
- **"专用驱动可以被完美替代"** → 是。驱动里真正有价值的只有 APO 注册，而这正被你复刻了。
- **"调音设置可以完美搬走"** → 是。耳机 EQ 与麦克风 EQ 都做到滤波器级等价，已验证。
- **唯一不完美的** → 360 空间音效 + ALC/DRC/限幅这类动态处理，属"可重建但需额外工作"。

**所以最准的表述是：**
> 索尼那套调音是纯软件，已被完整破译、可以完美搬到任何平台；但那个盒子不是"调音配件"，它是一块 DAC —— 替代它的方式只有换硬件，而这件事和调音可不可替代，是两件互不相干的事。
